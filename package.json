{
  "name": "lvllvl",
  "version": "0.496.1",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "check": "node --check scripts/build.mjs && node --check scripts/dev.mjs && node --check scripts/verify-build.mjs",
    "test": "npm run test:static && npm run test:browser",
    "test:static": "node scripts/verify-build.mjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "acorn": "8.18.0",
    "html-minifier-terser": "7.2.0",
    "terser": "5.51.2",
    "vite": "8.2.2"
  }
}
