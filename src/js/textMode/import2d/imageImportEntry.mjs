// Keep a mapped statement in this entry so build verification can prove that
// every declared feature source participates in the generated artifact.
const imageImportModule = true;

export { ImportImage, imageImportModule };
